spring.application.name=User-Service

# Development Database Configuration (PostgreSQL)
# Note: For production, use environment variables or external configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/snapskills_db
spring.datasource.username=prateekgulati
spring.datasource.password=Snap@123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8081

# Logging Configuration
logging.level.com.snapskills=DEBUG
logging.level.org.springframework.web=INFO

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
